export interface IEmployee{
    id?: number | null,
    name?: string | null,
    email?: string | null,
    phone?: string | null,
    age?: number | null,
    salary?: number | null,
    password?: string | null,
  }